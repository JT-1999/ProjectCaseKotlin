<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.projectcaseskot">

    <application
        android:name="com.example.ProjectCasesKotApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/MyTheme">
        <activity android:name=".ui.SelectNetActivity"></activity>
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".ui.ClipActivity" />

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.provider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider> <!-- DLNA服务 -->
        <service android:name="com.ykbjson.lib.screening.DLNABrowserService" /> <!-- 录屏服务 -->
        <service android:name="com.ykbjson.lib.screenrecorder.ScreenRecorderServiceImpl" />

        <activity
            android:name="com.ykbjson.lib.screening.RequestMediaProjectionActivity"
            android:theme="@style/RequestMediaProjectionActivityStyle" />
    </application>

</manifest>